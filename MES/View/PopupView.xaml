<Window x:Class="MES.View.PopupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:MES.View"
        xmlns:vm ="clr-namespace:MES.ViewModel"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="扫描材料码" Height="100" Width="800" ResizeMode="NoResize" Closed="Window_Closed">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction
            Command="{Binding WindowLoadedCmd}"
            CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.DataContext>
        <vm:PopupViewModel />
    </Window.DataContext>
    <Grid>
        <ItemsControl ItemsSource="{Binding MaterialCodeList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="1"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <DockPanel>
                            <TextBlock Text="{Binding MaterialName}"  Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                            <TextBox Text="{Binding MaterialCode}" Height="30" Margin="20,0,20,0" Padding="5"  HorizontalAlignment="Stretch" IsReadOnly="True"/>
                        </DockPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!--<Button Content="aa" Height="10" Width="10" Command="{Binding AddCommand}"/>-->
    </Grid>
</Window>
