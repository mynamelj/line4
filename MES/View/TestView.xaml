<Window x:Class="MES.View.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MES.ViewModel"
        mc:Ignorable="d"
        Title="PLC 调试工具" Height="550" Width="400" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:TestViewModel />
    </Window.DataContext>

    <Grid Background="#F5F5F5">
        <StackPanel Margin="30">
            <TextBlock Text="选择 PLC 组:" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding GroupList}" 
                      SelectedItem="{Binding SelectedGroup}" 
                      Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

            <TextBlock Text="标签名 (如: 产品型号_1):" Margin="0,5"/>
            <TextBox Text="{Binding TagName, UpdateSourceTrigger=PropertyChanged}" 
                     Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

            <TextBlock Text="数据类型:" Margin="0,5"/>
            <ComboBox ItemsSource="{Binding TypeList}" 
                      SelectedItem="{Binding SelectedType}" 
                      Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

            <TextBlock Text="写入数值:" Margin="0,5"/>
            <TextBox Text="{Binding WriteValue, UpdateSourceTrigger=PropertyChanged}" 
                     Height="35" VerticalContentAlignment="Center" Margin="0,0,0,20"/>

            <Button Content="执 行 写 入" 
                    Command="{Binding WriteCommand}" 
                    Height="45" 
                    FontSize="16" 
                    FontWeight="SemiBold"
                    Margin="5">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
                
            </Button>
            <Button Content=" 读 取 " 
                    Command="{Binding ReadCommand}" 
                    Height="45" 
                    FontSize="16" 
                    FontWeight="SemiBold"
                    Margin="5">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
