<Window x:Class="MES.View.LightConfigView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MES.View"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MES.ViewModel" xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="450"
             d:DesignWidth="800" WindowStartupLocation="CenterScreen"
             Background="White" Title="灯号配置"
             mc:Ignorable="d">
    <Window.DataContext>
        <vm:LightConfigViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ItemsControl ItemsSource="{Binding LightConfigList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <DockPanel>
                            <TextBox Text="{Binding LightNumber}" Margin="5" FontSize="20" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                            <TextBlock Text="{Binding LightCode}" Height="30" FontSize="15" Margin="10,0,10,0" Padding="5"  HorizontalAlignment="Stretch"/>
                        </DockPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <DockPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,5,10,5">
            <Button Content="保存配置" FontSize="15" Command="{Binding SaveLightConfigCmd}"/>
        </DockPanel>
    </Grid>
</Window>
