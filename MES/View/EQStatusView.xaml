<UserControl x:Class="MES.View.EQStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:MES.View"
             xmlns:vm="clr-namespace:MES.ViewModel"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:EQStatusViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center"
                    Grid.Column="0"
                    Grid.Row="0"
                    Margin="10"> 
            <TextBlock Text="分钉箱码扫描"
                       Margin="5"
                       HorizontalAlignment="Center" />
            <TextBox Text="{Binding BoxCode}"
                     Height="30"
                     Margin="20,0,20,0"
                     Padding="5"
                     HorizontalAlignment="Stretch"
                     TextAlignment="Center" />
        </StackPanel>

        <ItemsControl ItemsSource="{Binding BoxCodeInfo}"
                      Grid.Row="0" Margin="10" VerticalAlignment="Center"
                      Grid.Column="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <TextBlock Text="分钉箱码"
                                       Margin="5"
                                       HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding Index}"
                                       Margin="5"
                                       HorizontalAlignment="Center" />
                        </StackPanel>

                        <TextBox Text="{Binding Code}"
                                 Height="30"
                                 Margin="20,0,20,0"
                                 Padding="5"
                                 HorizontalAlignment="Stretch"
                                 TextAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Height="30"
                Margin="10"
                Width="100" 
                Grid.Column="1" 
                Grid.Row="1"
                Content="保存"
                Command="{Binding SaveCommand}" />
    </Grid>
</UserControl>
